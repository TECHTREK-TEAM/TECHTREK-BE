spring:
  application:
    name: techtrek
  datasource:
    url: jdbc:mysql://localhost:3306/techtrek
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true